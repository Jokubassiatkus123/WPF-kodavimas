<Window x:Class="FileEncryptionApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Encryption App" Height="350" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Pasirinkti failą su norimu veiksmu:" Grid.Row="0" Margin="10"/>
        <Button Content="Užkoduoti" Grid.Row="1" Margin="10" Click="EncryptButton_Click"/>
        <Button Content="Atkoduoti" Grid.Row="2" Margin="10" Click="DecryptButton_Click"/>
        <Label Content="Password:" Grid.Row="3" Margin="10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Margin="10"/>
    </Grid>
</Window>
